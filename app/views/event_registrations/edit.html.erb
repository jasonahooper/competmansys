<%= form_for [@event_registration.competition_attendee.competition, @event_registration.competition_attendee, @event_registration], :html => { :class => 'form-inline'} do |f| %>
  <%= render partial: "form", :locals => {:f => f} %>
  <%= f.button "Save" %>
<% end %>