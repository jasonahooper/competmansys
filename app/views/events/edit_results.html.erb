<%= form_for [@comp, @event], :html => { :class => 'form-inline' } do |f| %>
  <h3>Results for <%= @event.name %></h3>
  <%= f.fields_for :event_registrations do |r| %>
    <%= render partial: "event_registrations/form", :locals => {:f => r} %>
  <% end %>
  <%= f.button "Save" %>
<% end %>