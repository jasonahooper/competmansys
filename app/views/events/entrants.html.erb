<div id='event-entrant-index'>
  <div class="row">
    <div class="span9">
      <h2>List Entrants for Event: <%=@event.name%></h2>
        <ul>
          <% @entrants.each do |a| %>
            <li>
              <%= "#{a.full_name}" %>
            </li>
          <% end %>
        </ul>
    </div>
  </div>
</div>